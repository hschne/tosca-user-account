<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        >
    <Grid >
        <ListBox ItemsSource="{Binding Users}" HorizontalAlignment="Left" Height="100" Margin="329,33,0,0" VerticalAlignment="Top" Width="100">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" />
                        
                    </Grid>
                </DataTemplate>
                </ListBox.ItemTemplate>    
        </ListBox>
        <TextBox  HorizontalAlignment="Left" Height="23" Margin="242,271,0,0" TextWrapping="Wrap" Text="{Binding Path=TestString}" VerticalAlignment="Top" Width="120"/>

    </Grid>
</Window>
